const SearchBar = ({placeholder}) => {
    return ( //todo: add icon
      <input className="rounded-full shadow-md bg-white px-3 py-1 text-sm w-2/6" placeholder={placeholder} />
    );
};

export default SearchBar;
